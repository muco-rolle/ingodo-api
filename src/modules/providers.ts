import { userProvider } from '@modules/user';
import { authProvider } from '@modules/auth';
import { transactionProvider } from '@modules/transaction';

export const providers = { userProvider, authProvider, transactionProvider };
